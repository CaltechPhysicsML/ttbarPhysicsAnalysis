class hasParticle(object):

    def __init__(self, jetpt, jetbtag, electron, muon, met)
        self.jet = jet
        self.electron = electron
        self.muon = muon
        self.met = met
        
    def isElectron(self)
    
        if self.electron:
            print "true"
